local options = {
  ensure_installed = { "lua" , "go", "python", "terraform", "markdown", "markdown_inline", "javascript", "typescript", "prisma"},

  indent = { enable = true },

  highlight = {
    enable = true,
  },

}

return options
